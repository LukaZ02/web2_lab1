<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticket master</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="body">
    <div class="heading-container">
        <h1 id="heading">Ticket Master</h1>
    </div>
    <div class="navigation">
        <div class="navigation-buttons">
            <a href="/ticket/home"><button class="button-54">Home</button></a>
        </div>
        <div class="log-in-buttons">
            <% if(!isAuthenticated) {%>
                <a href="/login"><button class="button-54" id="only-log-in">Log in</button></a>
            <% } else { %>
                <div id="logged-in-username">Welcome, <%=name%></div>
                <a href="/logout"><button class="button-54">Log out</button></a>
            <% } %>
        </div>
    </div>
    <%- body %>
</body>
</html>
